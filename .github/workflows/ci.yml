name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    name: Build, Test & Package (Minecraft 1.21.1 / NeoForge 21.1)
    runs-on: ubuntu-latest

    env:
      MINECRAFT_VERSION: '1.21.1'
      NEOFORGE_VERSION: '21.1.209'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Set up Java 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Grant execute permissions for gradlew
        run: chmod +x ./gradlew

      - name: 1. Compile Java (1.21.1)
        run: ./gradlew compileJava

      - name: 2. Run Unit Tests (JUnit 5)
        run: ./gradlew test

      - name: 3. Run GameTests
        run: ./gradlew runGameTestServer

      - name: 4. Build Mod Jar (Minecraft 1.21.1)
        run: ./gradlew build

      - name: Upload Mod Artifact
        uses: actions/upload-artifact@v4
        with:
          name: peaceman_alpha-1.21.1-jar
          path: build/libs/*.jar
          if-no-files-found: error
